# プロジェクト仕様書：企画・屋台一覧（01_Events&Stalls_list）

最終更新日: 2026/02/22

---

## 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [対象ユーザー](#対象ユーザー)
3. [機能要件](#機能要件)
4. [画面設計](#画面設計)
5. [データベース設計](#データベース設計)
6. [API 設計](#api設計)
7. [非機能要件](#非機能要件)
8. [使用技術](#使用技術)
9. [セキュリティ設計](#セキュリティ設計)

---

## プロジェクト概要

### 目的

生駒祭における全企画および屋台の詳細情報（開催場所、カテゴリ、運営団体など）を一元管理し、運営スタッフが迅速かつ正確に情報を検索・閲覧できる「企画・屋台一覧モジュール」を提供する。
スタッフが業務上の確認を行う際や、来場者からの問い合わせに素早く対応できるよう、直感的でシンプルなUI（不要な装飾の排除、高速な検索・絞り込み）の提供を最優先とする。

### スケジュール

- 開発開始：2026/02/22
- リリース予定：未定（次回イベントに向け順次開発）

---

## 対象ユーザー

### 生駒祭運営スタッフ

- **いつ使うか：** イベントの準備期間から当日にかけて
- **どういうときに使うか：** 
  - 来場者から特定の企画や屋台について尋ねられたときの案内用
  - 運営スタッフ自身が特定の企画や屋台の詳細（場所、団体名など）を確認したいとき

---

## 機能要件

### 1. 一覧表示機能

**概要：**
Supabaseの `stalls`（屋台）および `events`（企画）テーブルから取得したデータを一覧形式で表示する。

**詳細仕様：**

- **タブ切り替え:** 画面上部のタブナビゲーションにより、「屋台一覧」と「企画一覧」を統合的かつシームレスに切り替えて表示する。
- **表示項目:** 各リストアイテムには、一目で識別できるよう「名称、カテゴリ、開催場所、運営団体名」をカード形式で表示する。

### 2. 検索・絞り込み・ソート機能

**概要：**
多数の出店・企画のなかから、目的の対象を素早く正確に特定するためのフィルタリングおよびソート機能を提供する。

**詳細仕様：**

- **あいまいキーワード検索:** 入力された文字列をもとに名称やキーワードを部分一致で検索する機能。
  - **横断検索スイッチ:** トグルスイッチにより、現在選択中のタブ内だけでなく「企画と屋台の両方を横断して検索」するモードに切り替えることができる。
- **カテゴリ絞り込み（ファセット検索）:** ピッカーから特定のカテゴリ（例：軽食、体験など）を選択し、一覧をそのカテゴリのみに絞り込む。
- **動的ソート機能:** ユーザーの目的に応じて、以下のキーによる一覧の並べ替え（昇順）をサポートする。
  - 名称順 (屋台・企画名)
  - 団体名順
  - カテゴリ順
  - 場所順

### 3. 詳細情報の表示機能

**概要：**
一覧から選択された項目の補足情報や詳細なテキストを、画面遷移を伴わずにユーザーへ提供する。

**詳細仕様：**

- 一覧の任意のカードをタップすると、画面下部からオーバーレイで詳細情報用**モーダルダイアログ**が表示される。
- 一覧に表示しきれない「詳細な説明文 (description)」等をフルテキストで提供する。
- **開催日程の最適化表示**:
  - 開催時間を最上部に大きく表示し、その下に「入場開始（準備時間）」と「退場完了（片付け時間）」を表示する。
  - **レスポンシブ対応**: PC等の大画面（600px以上）ではオプション時間を横並び（セパレーター「／」付き）で表示し、スマホ等の小画面（600px未満）では縦並びに自動切り替えを行う。
  - **視認性向上**: 時刻表示に等幅フォント（tabular-nums）を採用し、桁数や数字の種類によるズレを防止。コロン（:）の垂直位置を数字の中央に見えるよう微調整。
- ユーザーは「閉じる」ボタン、またはモーダル外のエリアタップにより直感的に元のリストへ戻ることができる。

---

## 画面設計

### 1. 企画・屋台一覧画面 (EventsStallsList01Screen)

**画面名:** EventsStallsList01Screen  
**パス/識別子:** `/01_Events&Stalls_list`

**表示項目:**

- 上部：検索バー（キーワード入力）、カテゴリ絞り込み（ピッカー等）、並べ替え順変更（ピッカー等）
- タブナビゲーション：「屋台一覧」「企画一覧」
- リストビュー（屋台または企画の一覧）
  - 各リストアイテムに表示する情報：
    - 屋台名 / 企画名
    - カテゴリ
    - 開催場所
    - 運営団体名

**動作:**

1. ユーザーがキーワードを入力すると、リストがフィルタリングされる。
2. カテゴリや並べ替え条件を変更するとリストが即座に反映される。
3. リスト内の特定の項目をタップすると、詳細モーダルが開く。

---

### 2. 詳細モーダル

**画面名:** DetailModal  

**表示項目:**

- 屋台名 / 企画名
- カテゴリ
- 開催場所
- 運営団体名
- **開催日程**:
  - 1行目: 開催時間（例: 10:00 - 12:00）
  - 2行目以降: オプション時間（入場開始 / 退場完了）。デバイス幅に応じて1〜2行で表示。
  - 垂直整列: 2行目以降のテキスト開始位置は、1行目の開始時刻の先頭と垂直に揃える。
- **説明文**（一覧には表示されない詳細情報）

**動作:**

1. 閉じるボタン、またはモーダル外の領域をタップするとモーダルが閉じる。

---

## データベース設計

※Supabase上のテーブルを利用する。本設計はアプリ側の想定構成であり、既存の構成に合わせて適宜マッピングを行う。

### 5.1. `stalls` テーブル
生駒祭での屋台データを格納する。

**主要カラム定義**:

- `id` (UUID): プライマリーキー
- `stall_organization_id` (UUID): `stall_organizations`への外部キー
- `location_id` (UUID): `stall_locations`への外部キー
- `name` (TEXT): 店名
- `description` (TEXT): 詳細説明文
- `image_path` (TEXT): サムネイル画像など
- `category_id` (TEXT): `stall_categorys`のid
- `is_published` (BOOLEAN): 開催・表示フラグ
- `created_at` (TIMESTAMPTZ)
- `updated_at` (TIMESTAMPTZ)

**表示項目へのマッピング**:
- 団体名 (グループ名) は `stall_organizations` テーブルの `name` を JOIN して表示。
- 出店場所は `stall_locations` テーブルの `name` と `building_locations` テーブルの `name` を組み合わせて表示。

### 5.2. `events` テーブル
生駒祭での企画（ステージ、展示、体験など）データを格納する。

**主要カラム定義**:

- `id` (UUID): プライマリーキー
- `name` (TEXT): 企画名
- `description` (TEXT): 詳細な説明文
- `image_path` (TEXT): 画像URL
- `category_id` (TEXT): `event_categorys` へのid
- `location_id` (UUID): `event_locations` への外部キー
- `event_organization_id` (UUID): `event_organizations` への外部キー
- `is_published` (BOOLEAN): 公開フラグ
- `created_at` (TIMESTAMPTZ)
- `updated_at` (TIMESTAMPTZ)

**表示項目へのマッピング**:
- 団体名 (グループ名) は、`event_organizations` テーブルの `name` を JOIN して表示。
- 出店場所は `event_locations` テーブルの `name` と `building_locations` テーブルの `name` を組み合わせて表示。

---

## API 設計

### 1. Supabase Client リクエスト

**アクセス方法:**
Erpプロジェクト内の既存のモジュールと同一の手法で作られた `src/services/supabase/client.js` 等の `supabase` オブジェクトを流用してデータを取得する。
データ取得フックとして `useEventsStallsList01Data` を実装する。

---

#### 屋台一覧取得 (Stalls)

**概要:**
`stalls` テーブルから一覧データ（屋台名、カテゴリ、開催場所、運営団体名、説明文等）を取得する。

#### 企画一覧取得 (Events)

**概要:**
`events` テーブルから一覧データ（企画名、カテゴリ、開催場所、運営団体名、説明文等）を取得する。

---

## 非機能要件

### 使いやすさ (Usability)

- 運営スタッフが急いでいる時でも迷わず使えるよう、無駄なアニメーションや過度な装飾は省く。
- 検索キーワードの入力から結果表示までのレスポンスが高速であること。機能性を重視する。

### パフォーマンス

- タブ切り替えやリストのスクロールがスムーズに行えること。
- 両テーブルのデータ読み込みを効率化し（可能であれば初回全件キャッシュ等）、待機時間を最小限にする。

---

## 使用技術

### フロントエンド

- **React Native (Expo)**
- **Tailwind CSS (NativeWind)** （装飾は必要最小限に留める）

### バックエンド

- **Supabase**
  - PostgreSQL データベース (`stalls`, `events` テーブル等)

---

## セキュリティ設計

### アクセス制御

- Erpシステムの認証を通過していることが前提。ログイン済みのユーザー（運営スタッフ）のみが情報を閲覧できる。
- Supabaseの `roles` テーブルにて `permissions.screens` に `企画・屋台一覧` の権限を持つユーザーのみアクセスが可能。

---

## 更新履歴

| 日付       | バージョン | 更新内容 | 更新者 |
| ---------- | ---------- | -------- | ------ |
| 2026/02/22 | 1.0        | 初版作成（ユーザー要件に基づき作成） | AI   |
| 2026/02/22 | 1.1        | カテゴリマスタの分離（`stall_categorys`, `event_categorys`）、場所マスタの分離（`stall_locations`, `event_locations`）、および `event_organizations` の構造変更を反映 | AI   |
| 2026/02/22 | 1.2        | モジュール名を `item1` から `01_Events&Stalls_list` に変更、関連コンポーネント・フック名の更新、Supabaseの画面権限名を `企画・屋台一覧` に変更した仕様を反映 | AI   |
| 2026/03/06 | 1.3        | カテゴリ参照を `category_id` に変更、場所表記に `building_locations` テーブル of `name` を統合表示する仕様を追加 | AI   |
| 2026/03/31 | 1.4        | 詳細モーダルの開催日程レイアウトを改善。開催時間を上、オプション時間を下に配置し、レスポンシブ対応（600px閾値）と等幅フォント（tabular-nums）適用を追加 | AI   |
