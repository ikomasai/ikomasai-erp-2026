# 管理部統合システム理解

最終更新: 2026-03-26  
作成: Codex  
対象仕様: [管理部統合システム仕様書.md](./管理部統合システム仕様書.md)

関連ドキュメント:
- [アプリ理解.md](./アプリ理解.md)
- [プロジェクト全体理解.md](./プロジェクト全体理解.md)
- [Codex編集履歴.md](./Codex編集履歴.md)

補足:
- 現在の全体理解の基準は [アプリ理解.md](./アプリ理解.md)。
- この文書は企画管理部統合システムに限定した補助理解ドキュメント。

---

## 1. このシステムの中核

管理部統合システムは、次の2本柱で運用する設計。

- 連絡案件（`support_tickets`）
  - 出展からの問い合わせ、緊急連絡、鍵申請、開始/終了報告などを一元管理
- 巡回タスク（`patrol_tasks`）
  - 連絡案件や鍵返却を起点に、巡回の受諾・完了・結果入力を管理

---

## 2. ロールと画面の対応（現実装）

| ロール | 画面 | 主機能 |
| --- | --- | --- |
| 出展（Exhibitor） | `item16` | 連絡登録、履歴確認、追記、添付 |
| 巡回（Patrol） | `item12` | タスク受諾/完了、巡回チェック、未巡回アラート、評価入力 |
| 本部（HQ） | `item13` | 全案件対応、ダッシュボード、巡回割当、鍵管理、評価タスク、無線 |
| 会計（Accounting） | `item14` | 会計対象案件の回答/状態更新、景品配布基準編集 |
| 物品（Property） | `item15` | 物品対象案件の回答/状態更新 |

補足:
- `item13`/`item14`/`item15` は共通コンポーネント `SupportDeskScreen` をロール別設定で利用。
- 通常の実装依頼では、この表にある `item12`〜`item16` と `support` を優先して確認する。

---

## 3. 主要業務フロー（実装ベース）

### 3.1 出展から連絡を送る

1. `item16` で質問/緊急/鍵申請/開始終了のいずれかを送信  
2. `exhibitorSupportService` が `support_tickets` を作成  
3. 種別に応じて通知
   - 会計系: `notify_target=accounting`
   - 物品系: `notify_target=property`
   - それ以外: 本部中心
4. 開始/終了報告は `rpc_create_ticket_and_auto_tasks` で巡回タスク自動生成（RPC未適用時はフォールバック）

### 3.2 巡回が対応する

1. `item12` で未割当または自分担当タスクを取得  
2. 「向かいます」: `acceptPatrolTask`（`rpc_accept_task` 優先）  
3. 「完了」: `completePatrolTask`（`rpc_complete_task` 優先）  
4. 結果は `patrol_task_results` へ記録し、必要時に元連絡案件へメッセージ反映

### 3.3 本部が統制する

1. `item13` で全連絡案件を横断確認  
2. 状態更新、回答、巡回割当、評価タスク生成、無線ログ登録
3. 鍵返却時は `rpc_return_key_and_create_lock_task` で施錠確認タスク生成

---

## 4. 仕様と実装の対応関係（主要ファイル）

- 出展入力/履歴:
  - `src/features/item16/screens/Item16Screen.jsx`
  - `src/features/item16/services/exhibitorSupportService.js`
- 本部/会計/物品対応:
  - `src/features/support/components/SupportDeskScreen.jsx`
  - `src/features/item13/screens/Item13Screen.jsx`
  - `src/features/item14/screens/Item14Screen.jsx`
  - `src/features/item15/screens/Item15Screen.jsx`
- 巡回対応:
  - `src/features/item12/screens/Item12Screen.jsx`
  - `src/services/supabase/patrolTaskService.js`
- 鍵運用:
  - `src/features/support/components/HQKeyManagementPanel.jsx`
  - `src/services/supabase/keyLoanService.js`
  - `src/services/supabase/keyReservationService.js`
- 通知:
  - `src/shared/services/supportWorkflowNotificationService.js`
  - `supabase/functions/dispatch-notification/index.ts`

---

## 5. ステータス定義（現実装で頻出）

- 連絡案件 `ticket_status`
  - `new`, `acknowledged`, `in_progress`, `waiting_external`, `resolved`, `closed`
- 巡回タスク `task_status`
  - `open`, `accepted`, `en_route`, `done`, `canceled`
- 巡回結果 `result_code`
  - `OK`, `NOT_STARTED`, `NOT_ENDED`, `NEED_SUPPORT`, `LOCKED`, `UNLOCKED`, `CANNOT_CONFIRM`

---

## 6. 2026-02-20 時点の実装メモ

- `item12`〜`item16` の主要導線は実装済みで、仕様書の核フローに概ね対応。
- 通知は「案件作成」「巡回受諾/完了」「施錠確認タスク作成」に接続済み。
- 2026-03-07 時点で、通知不達の一次原因は `dispatch-notification` 側ではなく、管理部統合システム利用アカウントの `push_subscriptions` 不足だった。
- 対策として `shared` を触らず、`item12` / `item13` / `item14` / `item15` / `item16` に Push 再登録カードを出し、画面表示時に Web Push 購読を再同期する導線を追加した。
- 2026-03-23 の修正で `push-subscription` は購読保存直後に同一ユーザーの古い `endpoint` を削除する。ローカル起動した関数に対して、既存購読 0 件のテストアカウントでダミー購読を 2 回登録し、2 回目で `removedCount: 1` と DB 上の最新 1 件残存を確認した。
- 返信投稿、ステータス更新、巡回担当割当の通知は `src/services/supabase/supportNotificationService.js` で補完し、既存 shared 通知ブリッジの外側で不足トリガーを埋めている。
- `dispatch-notification` は管理者向け個人通知に加え、連絡案件の返信/状態更新と巡回割当についてのみ、業務イベントを検証した上で個人宛て送信を許可する形へ拡張した。
- 2026-03-24 の追加調査で、会計対応・物品対応の返信/状態更新通知が 401 になる主因は `dispatch-notification` 側の受信者権限判定ではなく、クライアントが stale / revoked JWT を持ったまま Edge Function を呼ぶことだった。Edge Function は `auth.getUser()` で Auth セッションを厳密に見るため、DB 更新が通っても通知だけ `Invalid user token` で落ちうる。
- 追加調査で、Web Push 送信オプションに `notification.id`（UUID, 36文字）を `topic` ヘッダーとして渡していたことが全件失敗の直接原因候補と判明した。Topic 制約に抵触するため、この値は使わない。
- `item14` / `item15` の「対象連絡案件」は `support_tickets` / `ticket_messages` / `ticket_attachments` を Realtime 監視し、変更が入ると手動更新なしで再取得される。
- `item13` の本部サポートにある「企画一覧」、`item13` の評価対象企画一覧、`item12` の定常巡回チェック候補は、すべて同じ `events` を参照する。
- `item13` の評価タブで生成する評価タスクは `evaluation_checks` ではなく巡回サポート向け `patrol_tasks` に作成する。各企画について評価項目一覧を1件のタスクへまとめ、`notes` は `評価項目: ...` 形式で保存し、`item12` の `評価` タブで項目ごとの点数とコメントを入力する。
- 評価タスクの `notes` は、新規生成では評価項目配列に加えて `events.id` と団体名も持てる形で保持し、巡回サポート側は旧来の連結文字列形式に加えて旧「1項目1タスク」形式も後方互換で束ねて扱えるようにしている。
- `item13` の評価タブでは、未完了の「企画評価」タスクが1件でも残っている間は、新しい評価タスクを生成できない。
- `item13` の評価タブには `団体別評価` カードがあり、巡回サポートで登録した評価結果を団体ごと・企画ごとに点数とコメント付きで見返せる。
- 評価承認フローは廃止し、本部で生成した評価タスクを巡回担当がそのまま `item12` で受諾・完了する運用に変わった。
- `item13` の概況タブでは `企画報告確認` と `施錠確認` を個別に折りたためる。`施錠確認` は `担当あり` / `担当なし` と `確認済み` / `未確認` の組み合わせで絞り込める。
- `item13` の `巡回` タブでは、巡回タスク一覧からタスクを選ぶと `担当者選択` セクションまで自動スクロールする。選択後すぐに担当候補の切り替えと割当更新へ進める。
- `item13` の評価タブにある評価対象企画一覧は、団体名 / 企画名 / 場所で検索でき、`選択中だけ表示` に絞り込める。評価タスク一括生成ボタンは一覧の上側にも出し、候補数が多くても下端まで移動せずに生成操作へ入れる。
- `item12` では完了件数ランキングを廃止済みで、現在の下部タブは `タスク` / `チェック` / `評価` / `ダッシュボード` の4つに整理している。
- `item12` には団体別企画一覧向けの state / helper が一部残っているが、現行 UI では `企画一覧` タブは公開していない。
- `item12` の要約情報は `ダッシュボード` タブだけに置き、他タブでは概要カードを繰り返さない。巡回担当は要約を一度見たら、そのまま `タスク` または `チェック` に移って操作する構成にしている。
- `item12` の `タスク` タブは通常タスクだけを対象にし、タスク種別単位で折りたたみできる。企画名 / 場所 / 種別 / 鍵名で検索でき、一覧から選んだタスクは同じ `ScrollView` 内の `タスク詳細` セクションまで自動スクロールして受諾・完了操作へつなぐ。
- `item12` の `評価` タブは企画評価タスク専用で、企画名 / 場所 / 評価項目で検索できる。詳細では `notes` に入っている評価項目一覧を分解し、各項目ごとに点数とコメントを入力してから完了登録する。旧データで `1項目1タスク` に分かれている評価も、同じ企画単位でまとめて5項目入力できるよう画面上で統合表示する。
- `item12` の `巡回中` トグルは `user_profiles.on_patrol` を保持し、画面離脱やアプリのバックグラウンド移行では自動解除しない。初回表示時にDBの現在値を読み込み、その後は本人がトグル操作したときだけ更新する。
- `item12` の定常巡回チェックは `events.id` を巡回対象マスタとして参照し、団体名順に並んだ企画から1件を選んで登録する。本部サポートの「企画一覧」では同じ `events.id` に紐づく `patrol_checks` 履歴を確認できる。
- `item12` の定常巡回チェックは対象企画を選ぶとその企画だけ残して他候補を畳み、`キャンセル` で選び直す。チェック項目は 5 段階評価から設問別の選択肢入力へ切り替えた。
- `item13` の `連絡案件` タブで直接対応するのは現状 `rule_question` / `layout_change` で、`start_report` / `end_report` / `lock_check` は `概況確認` / `巡回` / `鍵管理` タブへ役割分担されている。`distribution_change` / `damage_report` は会計対応 / 物品対応側で扱う。
- `patrol_checks.check_items` は現在、各項目について `key / label / answerKey / answerLabel / memo` を持つ JSON 配列でも保存する。旧 `score` 形式や文字列配列は履歴表示だけ後方互換で読む。
- Web Push のポップアップ表示は `public/service-worker.js` が担当し、PWA を閉じている状態でも OS 通知として表示する前提にしている。
- `item16` の履歴画面は双方向チャットではなく、質問内容と管理部からの回答だけを見る QA 形式へ寄せている。
- `item16` の最新連絡案件はタブ別に `質問` / `緊急` / `鍵申請` / `開始終了` の対象種別だけを表示し、一覧全体を折りたためる。質問タブでは `企画ルール変更` / `配置図変更` / `商品配布基準変更` / `物品破損報告` の4種を出す。
- `item16` の質問タブでは、最新連絡案件の一覧から案件を選ぶと `質問詳細` まで自動スクロールする。
- `item16` の質問タブでは写真添付を出さず、写真が必要な相談は Discord 連絡へ案内する。緊急呼び出しと開始/終了報告でも添付は扱わず、開始/終了報告は報告種別の選択だけ送る形に簡略化した。
- `item16` の質問種別ボタンは対応先を表示せず、狭いスマホ幅では 1 列表示へ切り替えて長いラベルでも崩れないようにしている。
- `item14` の見た目上の状態は `未対応` / `対応中` / `完了` の3段階に固定し、`すべて` / `緊急のみ` / `通常のみ` の絞り込みは表示しない。内部保存値は既存 `support_tickets.ticket_status` を使い続ける。
- `item14` では会計担当が `prize_distribution` 一覧から対象の景品配布基準を選択し、ポップアップ編集モーダルで `distribution_criteria` を更新する。対象が多い日でも一覧を崩さず、その場で保存できる。
- `item15` の見た目上の状態は `未対応` / `対応中` / `対応済み` の3段階に固定し、緊急/通常フィルターは表示しない。内部保存値は既存 `support_tickets.ticket_status` を使い続ける。
- `item15` の詳細画面ではステータス変更を依頼本文より上に置き、通知到着後も画面復帰・フォーカス・Realtime で自動再取得する。
- 連絡案件通知は新規作成時も返信/状態更新時も、団体名・依頼者名・企画名・場所が先に見える文面へ統一した。
- 送信先ロールは `notify_target=property` が `物品部 + 管理者`、`notify_target=accounting` が `会計部 + 管理者`、それ以外が `企画管理部 + 管理者`。`emergency` / `start_report` / `end_report` は `警備部 + 企画管理部 + 管理者` に広げる。
- 2026-03-10 の修正で、新規案件通知と返信通知は `dispatch-notification` の `roleNames` 解決へ統一した。会計/物品宛ての新規通知は `supportWorkflowNotificationService`、返信/状態更新は `supportNotificationService` から同じ受信者決定ロジックを使う。
- `item14` / `item15` の案件詳細は、ステータス変更を確認ポップアップなしで即時反映する。回答送信時に自動で `in_progress` へ上げる場合も `notifyActorUserId` を渡し、通知文面と送信者判定を崩さない。
- 2026-03-23 の修正で、会計対応・物品対応の送信者画面は返信通知と状態更新通知の `push.attempted / push.succeeded / push.failed` を完了ダイアログへ表示する。配信先の未購読や配信失敗を、送信操作直後に画面上で追える。
- 2026-03-24 の修正で、Edge Function 呼び出し側は 401 を受けたら `refreshSession()` で最新セッションへ同期し直して 1 回だけ再送する。API レベルの確認では、`invalid-token` で `{"error":"Invalid user token"}` を返した同じ payload が、refresh token で取り直した access token では 200 と `push.succeeded=1` に復旧した。
- `item14` / `item15` は `対象連絡案件` と `案件詳細`、`item14` は加えて `景品配布基準` を折りたためる。作業量が多い日でも一覧と編集エリアを切り替えながら扱う前提にしている。
- `item14` の回答送信は確認ダイアログなしで即送信する。
- `item16` の画像添付は React Native の Blob をそのまま Storage に渡さず `ArrayBuffer` 化して送る。最新連絡案件にはタイトルと一覧プレビューの両方に質問詳細の先頭を載せ、似た種別が並んでも判別しやすくした。
- 仕様差分として要確認:
  - `item16` 送信時の `eventId` が現状 `null` 固定（仕様書では開始/終了報告時の `event_id` 必須）。
  - 設置型/持ち歩き分離は、現状「同一アプリ内のロール別画面」で実現。

---

## 7. この文書の更新ルール

- `item12`〜`item16`、`support_tickets`、`patrol_tasks`、`key_*`、通知フローを変えたら更新する。
- 変更時は先に `docs/アプリ理解.md` を更新し、その差分をこの文書へ反映する。
- 変更後は `docs/Codex編集履歴.md` にも同時記録する。


---

## 8. 2026-02-23 通知403の原因と対処

- 現象: `item16` から連絡案件を送信した際に `dispatch-notification` が `403 Forbidden` を返し、通知送信のみ失敗していた。
- 原因: Edge Function `dispatch-notification` 側の認可条件が実運用フローより厳しく、企画制作部アカウントの通知送信が拒否されていた。
- 対処: 認証済みユーザーの `targetType=roles` 通知を許可し、`targetType=user` は管理者ロールのみ許可する形へ修正。
- 反映: Supabase Edge Function `dispatch-notification` を version 8 としてデプロイ済み。
- 補足: 通知本文・受信者作成・Push送信の既存フローは維持。

---

## 9. 2026-02-27 アカウント作成メモ（企画者）

- 目的: 企画者（item16利用者）アカウントの追加作成。
- 実施結果:
  - Supabase MCP 接続は OAuth 更新エラーで失敗し、MCP経由のDB操作は未実行。
  - Supabase Auth `signUp` は成功（`test-exhibitor-20260227@gmail.com` 作成）。
  - ただしメール確認必須設定のため、確認前は `signInWithPassword` が `Email not confirmed` で失敗。
- 制約:
  - `user_roles` は仕様上 `SELECT` のみ公開で、匿名キー/一般ユーザー経由のロール付与は不可。
  - 企画者として即時利用するには、管理権限（service role またはダッシュボード管理者）で
    1) メール確認完了、2) `user_roles` へ企画者ロール付与、が必要。

## 2026-03-09 追記

- 権限方針を更新した。`item12` は `企画管理部 / 警備部 / 管理者`、`item14` は `会計部 / 管理者`、`item15` は `物品部 / 管理者`、`item13` は `管理者` のみを想定する。
- 企画管理部は巡回サポート中心の運用に寄せ、本部サポートは管理者確認用として扱う。
- `item14` / `item15` の通知はそれぞれ会計部・物品部を主対象にし、巡回系通知は `企画管理部 / 警備部 / 管理者` に送る。
- `item16` は QA 表示を維持したまま FAQ / 入力ヒントを外し、最新の連絡案件だけ利用者側でクローズできるようにした。
- 写真添付は `expo-image-picker` を追加し、ネイティブ端末からも写真ライブラリ経由で送信できるようにした。
- `dispatch-notification` は 403 調査をしやすくするため、会計/物品/本部/巡回/管理者のロール別名を認可対象へ広げ、個人宛て通知が拒否されたときは送信者・宛先・案件ID・ロール一覧を Edge Function ログへ残すよう更新した。会計/物品の回答通知とステータス更新通知を直すには、この関数の再デプロイが前提になる。
- 通知権限の仕様はさらに緩め、現在は `dispatch-notification` に有効な Bearer トークンまたは internal token が渡っていれば、管理部統合システムの業務種別に関係なく `user` / `roles` の両方へ送信できる前提に変更した。

## 2026-03-10 追記

- `item14` / `item15` の部署向け `SupportDeskScreen` では、案件一覧で案件を選択すると `案件詳細` まで自動スクロールするようにした。
- `item14` / `item15` の案件詳細ステータス更新は Picker ではなく 3 状態のボタン選択で更新する。
- 会計対応画面・物品対応画面の冒頭説明文は撤去し、物品対応画面の添付補足文も出さない。
- 本部サポートの `巡回タスク割当` は `企画管理部` ユーザーのみを担当者候補に出す。

## 2026-03-24 追記

- 会計対応・物品対応を含む `dispatch-notification` の送信経路で、各通知に `traceId` を付与して Edge Function ログを追えるようにした。ログには送信者プロフィール、受信者プロフィール、`targetType`、受信ロール指定、案件系メタデータ、`push_subscriptions` 件数、provider 別購読件数、`push.attempted / push.succeeded / push.failed / push.removed` を残す。
- これにより、通知レコード作成までは成功しているのか、受信者に購読がないのか、古い endpoint が掃除されたのか、provider 側送信で失敗したのかを `traceId` 単位で切り分けられる。

- 2026-03-24 の追加修正で、会計対応・物品対応・本部サポート・企画者サポートの送信系操作は通知送信前に `push-subscription` 再同期を挟む。既存購読があるブラウザでは `Notification.permission` が `default` / `denied` でも再同期を実行し、権限表示と購読保存を分離する。
- 2026-03-24 の追加修正で、個人宛て通知の認可は `dispatch-notification` 内で明示化した。認証済みユーザー同士の直接通知は許可しつつ、会計/物品の回答通知・状態更新通知など `support_ticket` 起点のものは、案件作成者と担当部署の関係をサーバー側で照合してから送る。
- あわせて、部署側の送信完了表示では `push.attempted=0` を `Push未送信` として出し、受信者に Web Push 購読がない状態を UI 上でも切り分けられるようにした。
- 2026-03-24 の追加修正で、個人宛て通知の認可はさらに単純化し、`dispatch-notification` は認証済みユーザー同士なら管理者以外でも `user -> user` を常時許可する。これにより、会計対応・物品対応でのメッセージ返信やステータス変更通知は、業務イベント種別による制限なしで個人宛て送信できる。
- 2026-03-24 の追加修正で、`dispatch-notification` の個人宛て認可は `userId` の存在確認だけに整理した。これにより、管理者・部署・業務イベントの別を問わず、相手のユーザーIDを指定した `user -> user` 通知は常時許可される。
- 2026-03-24 の追加修正で、組織宛て通知も `dispatch-notification` で扱えるようにした。`roleNames` または `roleIds` に加えて `organizationId(s)` / `organizationName(s)` を渡すと、`user_roles` でロール一致した候補から `user_organizations` に一致するユーザーだけを受信者に残す。
- クライアント側には `sendNotificationToOrganizationRoleNames(...)` を追加し、「相手組織 + 相手ロール」が分かっている場合の通知送信を共通化した。
- 2026-03-24 の追加修正で、`dispatch-notification` は `organization` 宛て送信を追加し、`user_organizations` にぶら下がる対象組織メンバー全員へ通知できるようにした。これで「個人が分かれば個人」「組織しか分からなければ組織全体」の両方をサーバー側で扱える。
- `supportNotificationService` は出展団体向けの返信通知・状態更新通知で、個人宛て通知の Push 成功が 0 件なら組織通知へ自動フォールバックする。
- 2026-03-24 の追加修正で、管理部統合システムの個別画面へ入っていない一般ユーザーでも Web Push を有効化できるよう、`AppNavigator` に全ログインユーザー共通の Push 有効化バナーを追加した。既存の `item12` / `item13` / `item14` / `item15` / `item16` は画面内バナーを維持し、それ以外の画面ではアプリ上部の共通バナーから `push-subscription` の再登録を実行する。
- 2026-03-24 の追加修正で、共通バナー・画面内バナーの両方がブラウザ `focus` 復帰時に `push-subscription` を再確認するようにした。これにより、通知権限を手動で許可へ戻した直後でも、再読み込みなしで購読状態を UI に反映しやすくした。
- 2026-03-25 の追加修正で、会計対応・物品対応の返信通知と状態更新通知は、迷子通知の登録主通知と同じく `support_tickets.created_by` を個人通知先に固定した。これにより、団体内の別メンバーへ宛先がぶれる経路をなくし、案件作成者本人への `user -> user` Push を優先できる。
- 2026-03-25 の追加修正で、上記 2 通知は `allowOrganizationFallback: false` を維持し、組織宛てフォールバック成功が個人宛て Push 成功として表示されないようにした。送信側 UI の `push.succeeded` は案件作成者本人への Web Push 成功件数だけを示す。
- 2026-03-25 の追加修正で、`dispatch-notification` は送信レスポンスへ `traceId`・送信者・解決済み受信者一覧を返し、送信ブラウザ側の `notificationService` は `[notification][browser]` ログとして console に記録するようにした。会計対応・物品対応の送信者は、ブラウザログから「誰が誰へ送ったか」を即確認できる。
- 2026-03-25 の追加修正で、`service-worker.js` と `useWebPushDebugListener` を追加し、受信ブラウザ側では `push_received` / `notification_shown` / `notification_show_error` を通常の browser console でも確認できるようにした。PWA の Service Worker console だけでなく、開いている画面の console でも受信経路を追える。
- 2026-03-25 の追加修正で、`push-delivery-receipt` Edge Function を追加し、Service Worker が `showNotification` の前後で受信 ACK を返すようにした。Supabase の function log では `traceId` ごとに送信者・受信者・受信ブラウザの userAgent・表示成功/失敗が残る。
- 2026-03-25 の追加修正で、`dispatch-notification` と `push-subscription` の認証は request の `Authorization` ヘッダーをそのまま引き継いだ `SUPABASE_ANON_KEY` クライアントで `auth.getUser()` する方式へ統一した。従来の `service role client + auth.getUser(token)` だとブラウザ側では有効に見えるセッションでも Edge Function だけ 401 になるケースがあり、今回そこを Supabase 推奨の user-auth 検証経路へ寄せた。
- 2026-03-25 の追加修正で、`supabase/config.toml` を追加し、`dispatch-notification`・`push-subscription`・`push-delivery-receipt` の `verify_jwt = false` をコード管理に移した。Edge Function の `Verify JWT with legacy secret` を Dashboard で手動で戻してしまっても、CLI 再デプロイ時に同じ設定へ揃う。
- 2026-03-25 の追加修正で、管理部統合システムの Push をアプリ起動中に受信した場合は `AppNavigator` が `SW_PUSH_DEBUG` の `notification_shown` / `notification_show_error` を受けてアプリ内トーストを表示するようになった。Windows の OS トーストが見えなくても、会計対応・物品対応を含む受信結果を画面上で確認できる。
- 2026-03-25 の追加修正で、会計対応・物品対応を含む管理部統合システムの Push は、アプリを閉じている間に届いた場合でも `service-worker.js` が保持し、次回起動時に `useWebPushDebugListener` 経由でアプリ内トーストとして再表示するようになった。これにより、OS トーストを見逃しても復帰後に受信確認できる。
## 2026-03-30 修正メモ

- 会計対応は SupportDeskScreen 内で 連絡案件 / 景品配布基準 の2タブ構成になった。物品対応・本部サポートには影響せず、会計ロール時だけタブ切り替えを表示する。
- 本部サポートの 企画報告確認 は巡回確認タスクではなく、企画者サポートから作成される support_tickets.start_report / support_tickets.end_report を直接監視する構成へ補正した。
- 本部サポートの 施錠確認 は overview 専用ロードを追加し、進行中タスク一覧とは別に完了済み・取消済みも保持する。これにより 完了済み フィルターで該当件数が正しく出る。
- 本部サポートの現地対応振り分けは、すでに移動中/受諾中の巡回タスクを持つ企画管理部メンバーを選択不可にした。
- 巡回サポートの詳細では、HQ から振り分けられた連絡案件由来タスクについて元案件の依頼内容をそのまま確認できる。

## 2026-03-31 修正メモ

- 巡回サポートの `タスク詳細` では、`登録完了` を `accepted / en_route` の巡回タスクだけに制限した。巡回タスク一覧で `向かいます` を押していない `open` タスクは完了登録できない。
- 本部サポートの `概況確認 > 巡回中スタッフ` は、`on_patrol=true` のスタッフごとに未完了タスクを束ねて表示する。各タスクで `移動中 / 向かい中 / 割当済み`、タスク種別、企画名・場所、依頼内容要約、受諾または作成時刻、`task_no` を確認できる。
- 企画者サポートの `緊急` タブ送信完了文言は `企画管理部に連絡案件を送信しました。` に変更し、画面文言と実運用の宛先表現を一致させた。
- 本部サポートの `巡回タスク割当` は、すでに `open / accepted / en_route` の巡回タスクを担当している企画管理部メンバーを新規割当先に選べない。候補UIだけでなく、巡回割当更新・振り分けタスク生成・独自タスク生成のサービス側でも多重割当を拒否する。
- 本部サポートの `巡回タスク割当` には、選択中タスクの削除ボタンを追加した。削除時は確認ダイアログを挟み、関連する `patrol_task_results`、旧 `evaluation_checks.task_id`、`key_loans.lock_task_id` 参照も一緒に整理する。
- 企画者サポートの `鍵の事前申請` は、借受人・団体を `鍵貸出/返却端末` と同じ候補選択UIへ変更した。保存時は `key_reservations.metadata` と `support_tickets.metadata` に借受人/団体の ID と名前を残す。
- 本部サポートで鍵予約承認から貸出記録を作る処理は、上記メタデータの `borrower_person_name` / `borrower_org_name` / `borrower_org_id` を優先して使う。これにより、事前申請と貸出/返却端末で検索・照合に使う名前と組織IDが揃う。
- 企画者サポートの `鍵の事前申請` は、`key_loans` のうち `status='loaned'` の鍵を候補一覧で見分けられるようにし、貸出中の鍵は追加も送信もできない。フォーム表示中に状況が変わっても、送信前の再確認で二重申請を防ぐ。
- 2026-03-31: 本部 `overview` の `企画報告確認` は一覧だけでなく詳細確認カードを持ち、開始/終了報告のメッセージ確認と関連 `confirm_start / confirm_end` タスクへの遷移をここから行う。
- 2026-03-31: 本部 `overview` の `施錠確認` はカード選択で詳細表示し、進行中 `lock_check` を `patrol` タブの割当画面へ開ける。完了・取消済みは概要表示のみで扱う。
- 2026-03-31: 鍵管理の借受人単位 `全部返却` は完全返却として扱い、返却した鍵ごとに `lock_check` タスク生成対象になる。
- 2026-03-31: 本部サポートの `鍵管理` では、`施錠確認状況` の未確認行に対して `lock_task_id` 未設定の返却済み鍵だけ `施錠確認タスクを作成` を出し、既存データにも巡回依頼を補完できる。サービス側は `createLockCheckTaskForReturnedLoan(...)` で返却状態を再更新せず、active (`open / accepted / en_route`) な既存タスクがあれば再利用する。
- 2026-04-05: 本部が設定する評価フォームURLは共有設定テーブル `ERP_setting` の `evaluationFormUrl` に保存する。巡回サポートは端末ローカルではなくこの共有値を読むため、本部端末で更新したURLを他の巡回端末でも利用できる。
